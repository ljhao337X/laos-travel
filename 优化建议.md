# 优化建议

！！！这个文件只允许我人工亲自改！！！

## 地点池

拖拽功能无法用，没法拖拽到右侧的安排表中 -- 解决 不用拖拽了

需要有一个位置展示这个项目的具体信息 -- 解决


### 地点池抽签功能

我使用tag筛选之后，应该只能从对应的地点池里抽签 -- 解决

tag设置两级，第一级 吃的/玩的/逛的 ，第二级设置 吃的--正餐/小吃 等等 -- 解决

老虎机的显示有问题，结果是对的。动画中有非当前活动的选项。


## 新增修改项

添加到时间线时，如果选择了早上下午晚上，如果对应时间段有活动，则加在最后，如果对应时间段没有活动，则以对应时间段的开始时间作为开始时间加入到时间线中。

活动持续时间可以修改。在详情页面里。

## 权限管控
输入某个密码之后，信息同步才会生效，否则只是一个暂时的前端页面

缺少退出游客状态或者退出登录状态的按键。

增加一个账户名的功能，比如XXL，密码是同一个。

## 增加功能

增加一个地点池生成器，同步共享

增加一个链接池，可以把感兴趣的链接一条条加进来，同步所有人看见。


